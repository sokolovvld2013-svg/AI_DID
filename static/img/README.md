# Изображения интерфейса

Положите в эту папку (или укажите пути в `.env`):

- `logo.png` — логотип в шапке
- `favicon.png` — иконка вкладки браузера

При старте приложение копирует их из `LOGO_SOURCE` / `FAVICON_SOURCE`, если заданы в `.env`.
