# Локальные эмбеддинги (EMBEDDING_PROVIDER=local).
# Сначала установите основные зависимости: pip install -r requirements.txt
# PyTorch CPU-only — без пакетов nvidia_* (~2 ГБ экономии на VPS без GPU).
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers>=2.3.0,<3.0.0
